import DefaultBackgroundProps from "../../fields/background/backgroundDefaultProps";
import FormProps from "./type";

const DefaultFormProps: FormProps = {
  collection: undefined,
  background: DefaultBackgroundProps,
  theme: "light",
};
